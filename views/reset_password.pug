extends layout

block content
  form(method='POST')
    legend Reset Password
    .form-group
      label(for='password') Nouveau mot de passe
      input#password.form-control(type='password', name='password', value='', placeholder='Nouveau mot de passe', autofocus=true)
    .form-group
      label(for='password2') Confirmation du mot de passe
      input#password2.form-control(type='password', name='password2', value='', placeholder='Confirmation du mot de passe')
    .form-group
      button.btn.btn-primary(type='submit') Mettre à jour le mot de passe